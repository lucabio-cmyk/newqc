# RCA / CAPA service dependencies.
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
pydantic>=2.6,<3
prometheus-client>=0.20,<1
loguru>=0.7,<1

# Optional: enable the Neo4j-backed knowledge graph (set NEO4J_URI to use it).
# The service runs on the in-memory graph without it. Uncomment to install:
# neo4j>=5.18
