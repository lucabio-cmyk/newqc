# ML-Inference service dependencies.
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1
pydantic>=2.6,<3
numpy>=1.26
prometheus-client>=0.20
loguru>=0.7

# Optional extra: enables the isolation-forest anomaly-detection upgrade path.
# The default robust (median/MAD + IQR) path does NOT require this.
# scikit-learn>=1.4

# Dev/test dependencies (not installed in the runtime image):
#   pytest>=8  black>=24  ruff>=0.4  httpx>=0.27
