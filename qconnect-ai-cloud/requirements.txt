# =====================================================================
# QConnect-AI Cloud :: aggregate runtime + dev requirements
# ---------------------------------------------------------------------
# This is the umbrella requirements file used by CI and local dev. Each
# service also ships its own minimal requirements.txt for slim images.
# =====================================================================

# --- web / API ---
fastapi>=0.110,<1
uvicorn[standard]>=0.29,<1

# --- data models / settings ---
pydantic>=2.6,<3
pydantic-settings>=2.2,<3

# --- persistence ---
sqlalchemy[asyncio]>=2.0,<3
asyncpg>=0.29
# migrations (Alembic runs synchronously; psycopg is the sync driver)
alembic>=1.13
psycopg[binary]>=3.1

# --- clients / messaging ---
httpx>=0.27
redis>=5.0

# --- scientific stack ---
numpy>=1.26
scipy>=1.12

# --- graph / RCA ---
neo4j>=5.18

# --- observability ---
loguru>=0.7
prometheus-client>=0.20

# --- security ---
PyJWT>=2.8

# --- dev / test (installed in CI) ---
pytest>=8.0
pytest-asyncio>=0.23
black>=24.0
ruff>=0.3
