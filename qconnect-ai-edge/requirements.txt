# =============================================================================
# QConnect-AI Edge - Python dependencies
# -----------------------------------------------------------------------------
# Deliberately dependency-light: the inference engines and cache are pure-stdlib.
# Heavy ML stacks are NOT required at import time and are optional (see bottom).
# Pinned to minimum versions; Python 3.11+ required.
# =============================================================================

# --- Web / API (qc_inference) ---
fastapi>=0.110
uvicorn[standard]>=0.29

# --- Data validation / settings ---
pydantic>=2.6
pydantic-settings>=2.2

# --- HTTP client (cloud sync, health probes) ---
httpx>=0.27

# --- Logging ---
loguru>=0.7

# --- Observability / metrics ---
prometheus-client>=0.20

# --- Local dashboard (web_ui) ---
flask>=3.0

# --- Config files ---
pyyaml>=6.0

# =============================================================================
# OPTIONAL: on-device ML inference for the LSTM-lite forecaster.
# These are NOT installed by default and are NOT required - the edge falls back
# to a deterministic heuristic when no runtime/model is present. Install ONE of
# the following on edge hardware that ships a trained model:
#
#   tflite-runtime>=2.14      # lightweight, recommended for ARM/edge devices
#   tensorflow>=2.14          # full TF (heavier; x86 servers only)
#
# Left commented so the base image stays small and offline-buildable.
# =============================================================================
