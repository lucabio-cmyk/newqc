"""Cross-project integration tests for the QConnect-AI monorepo.

Named ``integration_tests`` (not ``tests``) so it never collides with the three
per-project ``tests`` packages when collected in the same pytest invocation.
"""
